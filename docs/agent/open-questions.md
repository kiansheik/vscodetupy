# Open Questions

- Does the user have a global or language-specific `editor.inlineSuggest.suppressSuggestions` user setting that overrides the extension's Tupy default?
- Should inline `var.` search eventually move from completion items to a custom webview/quick-pick flow if Copilot continues to interfere with VS Code's native suggest widget?

